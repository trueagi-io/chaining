[project]
name = "argset-chaining"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "hyperon>=0.2.2",
    "sortedcontainers>=2.4.0",
]
